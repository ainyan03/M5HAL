// Intentionally empty: this project only verifies that a plain downstream
// application can RESOLVE and BUILD M5HAL through the component manager.
// API-surface compilation is covered by test/idf_component_build.
void app_main(void)
{
}
